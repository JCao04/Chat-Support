# Root Environment Variables (used by monorepo scripts)

# Node Environment
NODE_ENV=development

# Database (shared across API and migrations)
DATABASE_URL="postgresql://postgres:password@localhost:5432/resume_review_dev?schema=public"
